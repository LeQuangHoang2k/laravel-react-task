import React from 'react';
import PropTypes from 'prop-types';

Body.propTypes = {
    
};

function Body(props) {
    return (
        <div>
            Body
        </div>
    );
}

export default Body;